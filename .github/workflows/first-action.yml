name: First Workflow
on: workflow_dispatch
jobs:
  first-job-name:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World"
      - name: Run multi-line Shell commands
        run:  |
            echo "First output"
            echo "Second output"
        
      - name: Print goodbye
        run: echo "Done - bye"
  
